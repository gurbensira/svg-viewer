{
  "name": "svg-viewer-backend",
  "version": "1.0.0",
  "description": "SVG Viewer backend — Node.js + TypeScript + Express + MongoDB",
  "main": "dist/app.js",
  "scripts": {
    "dev": "cross-env NODE_ENV=development ts-node-dev --respawn --transpile-only src/app.ts",
    "build": "tsc",
    "start": "cross-env NODE_ENV=production node dist/app.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "fast-xml-parser": "^5.3.7",
    "mongoose": "^8.4.1",
    "multer": "^2.0.2"
  },
  "engines": {
    "node": "22.x"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/multer": "^2.0.0",
    "@types/node": "^25.3.0",
    "cross-env": "^10.1.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.5"
  }
}
